{% set minSuffix = '.min' %}
{% if ceresConfig.log.performanceLevel  == 'development' %}
    {% set minSuffix = '' %}
{% endif %}

<link href="{{ plugin_path('VariationCrossSelling') }}/css/main{{ minSuffix }}.css?v={{ buildHash }}" rel="stylesheet">
